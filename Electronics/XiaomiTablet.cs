namespace Electronics
{
    internal class XiaomiTablet : Tablets
    {
        public XiaomiTablet(string manufacturer, string model, string color, decimal price, string guarantee, string memoryCapacity, string workspaceSize, string format, string pressureSensitivity, string responseSpeed)
            : base(manufacturer, model, color, price, guarantee, memoryCapacity, workspaceSize, format, pressureSensitivity, responseSpeed)
        {
        }
    }
}
